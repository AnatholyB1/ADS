interface webgpuProps extends React.HTMLAttributes<HTMLDivElement> {
    // props
}
export default function Webgpu(props: webgpuProps) {
    return (
        <section {...props}>
            <h1>webgpu</h1>
        </section>
    );
}